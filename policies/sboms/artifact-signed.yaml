# name: my_policy
rules:
  - name: artifact-signed
    evidence: 
      signed: false
      format-type: cyclonedx-json
      filter-by:
        - product
      target_type: image
    with:
      identity:
        common-names:
          - EvidencePolicyResults4
