# name: my_policy
enable: true
modules:
  - name: artifact-signed
    type: verify-artifact
    enable: true
    input:
      signed: true
      format: attest-cyclonedx-json
      context-group:
        - flags
      identity:
        common-names:
          - EvidencePolicyResults4
      match:
        target_type: image
