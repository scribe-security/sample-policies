# name: slsa-policy
rules:
- name: builder
  path: verify-builder.rego
  evidence: 
    signed: false
    format-type: slsa
    filter-by:
      - product
    target_type: image
  with: 
    id: '{{ .Args.BUILDER_ID }}'
