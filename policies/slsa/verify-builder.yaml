# name: slsa-policy
enable: true
modules:
- name: builder
  type: verify-artifact
  enable: true
  input:
    signed: true
    format: attest-slsa
    context-group:
      - flags
    match:
      target_type: image
    rego:
      args:
        id: "local"
      path: verify-builder.rego
