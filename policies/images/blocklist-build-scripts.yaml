# name: image-policy
rules:
- name: blocklist-build-scripts
  path: blocklist-build-scripts.rego

  evidence:
    signed: false
    format-type: cyclonedx-json
    filter-by:
      - product
    target_type: image

  with:
    blocklist: 
      - curl
